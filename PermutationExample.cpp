// 오름차순
for(int i = 0; i < n; i++)
{
  v.push_back(a[i]);
}

do
{
  // Print or . . . . Logic . . . 
}while(next_permutation(v.begin(), v.end());


// 내림차순
for(int i = 0; i < n; i++)
{
  v.push_back(a[i]);
}

do
{
  // Print or . . . . Logic . . . 
}while(prev_permutation(v.begin(), v.end());

