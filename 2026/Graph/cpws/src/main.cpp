#include <bits/stdc++.h>
using namespace std;

/*
cmake -S . -B build
cmake --build build -j
./build/main
*/

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    std::cout << "Hello, World!" << std::endl;

    // solve here
    return 0;
}
